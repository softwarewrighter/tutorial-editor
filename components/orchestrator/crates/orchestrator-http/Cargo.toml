[package]
name = "orchestrator-http"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = { workspace = true }
warp = { workspace = true }
tracing = { workspace = true }
orchestrator-app = { path = "../orchestrator-app" }
orchestrator-http-health = { path = "../orchestrator-http-health" }
orchestrator-http-project = { path = "../orchestrator-http-project" }
orchestrator-http-scene = { path = "../orchestrator-http-scene" }
orchestrator-http-asset = { path = "../orchestrator-http-asset" }
orchestrator-http-avatar = { path = "../orchestrator-http-avatar" }
orchestrator-http-script = { path = "../orchestrator-http-script" }
